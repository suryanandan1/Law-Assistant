# Everything needed to run ingestion AND the app on one machine.
# For a leaner install, use requirements-app.txt or requirements-ingest.txt directly.
# For a reproducible install, use the pinned tree in requirements.lock
# (uv pip sync requirements.lock  /  pip install -r requirements.lock).

-r requirements-app.txt
-r requirements-ingest.txt
